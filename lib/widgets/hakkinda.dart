import 'package:flutter/material.dart';

class Hakkinda extends StatelessWidget {
  const Hakkinda({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Hakkında', style: TextStyle(fontSize: 20));
  }
}
